{# templates/user/user.html.twig #}
{% extends 'base.html.twig'%}

{% block body %}
<body>
    <p>Bienvenue sur ton profil {{ app.user.username }}</p>

    <h1>Voici la liste de toutes nos oeuvres :</h1> 
        <a href="{{ path('works') }}">Ajoutez une oeuvre à votre collection</a>

    <h1>Voici votre collections :</h1>
    
    {% for item in collection %}
        <p>{{ item.name }}</p>
        <a href="{{ path('collection', {id: item.id}) }}">Collection</a>
    {% endfor %}
    </div>
</body>  
{% endblock %}